---
title: '算法 --- 递归和尾递归'
categories:
- 算法
tags:
- 递归



---

内容简介 : 尾递归节省内存

---

- 试题内容  ---  尾递归

  通常使用递归实现斐波那契问题,在程序的内存之中需要存储n + ( n-1) + (n -2) +... + fib(1) ,然而使用尾递归只需要存储 n总 + fib(1),大大节省内存,在递归的途中就已经实现了问题的解决。

  尾递归比线性递归通常多一个参数,就是上一次函数用到的结果,因此,尾递归并不依赖上层函数运行的余下结果,可以有效避免栈溢出,节省空间内存。

---

* 思路：归并加上不断迭代，解决系统栈问题
* 我的代码 --- 使用尾递归实现斐波那契数列

```
#include<iostream>
#include<cstring>
#include<cstdio>
#include<algorithm>
#include<cmath>
using namespace std;

int fibonacci(int n,int a1,int a2)
{
    if(n == 1)
        return a1;
    else
        return fibonacci(n-1,a2,a1 + a2);
}

int main(){
    
    int n;
    cin >> n;
    cout << fibonacci(n,1,1) << endl;
    
    return 0;
}
```



---


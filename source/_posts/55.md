---
title: '进制转换 2.0'
categories:
- 新生选拔赛
tags:
- 20级新生选拔天梯赛( 一 )
- 模拟进制





---

内容简介 : 基础题

* 出题人 : 杨宇

---

- 试题内容  ---  进制转换 2.0

## 题目描述

小紫的老师给小紫出了一道进制转换的题目：将十进制数转换成二进制数表示，这对于学过专业导论的小紫来说非常简单，小紫按照导论书上给出的算法思路，很容易就写出了对应的C语言代码，并成功地完成了题目。现在，对于参加天梯赛的你们来说，这显然也是小菜一碟，所以我们要加大力度。

## 输入

输入一共有2行。
第1行是一个正整数n和一个数s，n表示输入的数s是n进制数.
第2行是一个正整数m，m表示用m进制输出数s。

## 输出

将数s转换成m进制然后输出。

## 样例输入

```
10 1000
16
```

## 样例输出

```
3E8
```



## 提示

数据约定：

对于50%的数据：

2 ≤ n, m ≤ 16, n ≠ m

-105 ≤ s ≤ 105

对于100%的数据：

2 ≤ n, m ≤ 36, n ≠ m

-1018 ≤ s ≤ 1018

---

* 我的代码

```
#include<stdio.h>
#include<string.h>
#include<stdlib.h>
long TenNum(char a[],long long B);
void Numchange(long long m, int B); 
long TenNum(char a[], long long B)
{
    int len, i, num;
    long sum = 0;     
    len = strlen(a);
    for (i = 0; i < len; i++)
    {
        if (a[i] >= '0' && a[i] <= '9')
            num = a[i] - '0';
        else if (a[i] >= 'A' && a[i] <= 'Z')
            num = a[i] - 'A' + 10;
        sum = sum * B + num;
    }
    return sum;
}
void Numchange(long long m, int B)
{
    int n;
    if (m ) 
    {
        Numchange(m / B, B);
        n = m % B;
        if (n < 10)
        {
            printf("%d", n); 
        }
        else
        {
            printf("%c", n + 55);
        }
    }
}
int main()
{
    long long n, s;
    char a[22];    
    scanf("%lld %s", &n,a);
    long long m = 0;
    scanf("%d", &s);
    if(a[0]=='-')
    {
    m=TenNum(a+1,n);
    printf("-");
    }  
    else
    {
        m = TenNum(a,n);
       }   
    Numchange(m , s);  
    printf("\n");
    return 0;
}
```



---

